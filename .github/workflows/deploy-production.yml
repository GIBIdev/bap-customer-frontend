name: Deploy Customer Frontend

on:
  push:
    branches:
      - production-ui
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    env:
      AWS_REGION: us-east-2
      S3_BUCKET: bouf-a-laport-customer-prod-2026
      CLOUDFRONT_DISTRIBUTION_ID: EW397YH6PPS1
      VITE_API_URL: /api/v1

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build production frontend
        run: npm run build

      - name: Verify build
        run: |
          test -f dist/index.html
          echo "Frontend build successful"

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v6
        with:
          role-to-assume: arn:aws:iam::487694860774:role/GitHubActionsBapFrontendDeploy
          aws-region: ${{ env.AWS_REGION }}
          role-session-name: bap-frontend-deploy

      - name: Deploy to S3
        run: |
          aws s3 sync dist/ s3://${S3_BUCKET}/ \
            --delete \
            --region ${AWS_REGION}

      - name: Invalidate CloudFront
        run: |
          aws cloudfront create-invalidation \
            --distribution-id ${CLOUDFRONT_DISTRIBUTION_ID} \
            --paths "/*"

      - name: Deployment complete
        run: |
          echo "Deployment successful"
          echo "https://decjcog858se8.cloudfront.net"